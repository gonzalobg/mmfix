C coRR-na-mp
{ [x] = 0; }

P0 (int* x) {
  *x = 1;
  atomic_store_explicit(x, 2, memory_order_relaxed);
}

P1 (int* x) {
  int r1 = atomic_load_explicit(x, memory_order_relaxed);
  if (r1 == 2) {
    int r2 = *x;
  }
}

exists (1:r1=2 /\ ~1:r2=2 /\ undef)