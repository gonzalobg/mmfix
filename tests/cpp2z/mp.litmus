C coRR-na-mp
{ [x] = 0; [f] = 0}

P0 (int* x, int* f) {
  *x = 42;
  atomic_store_explicit(f, 1, memory_order_relaxed);
}

P1 (int* x, int* f) {
  int r1 = atomic_load_explicit(f, memory_order_relaxed);
  if (r1 == 1) {
    int r2 = *x;
  }
}

exists (1:r1=1 /\ 1:r2=0)