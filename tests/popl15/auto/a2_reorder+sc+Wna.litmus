C a2_reorder+sc+Wna
{ [x] = 0; [y] = 0; [zero] = 0; }

P0 (int* x, int* y) {
  SCAS(x, zero, 1, memory_order_seq_cst, memory_order_relaxed);
  *y = 0;
}

P1 (int* x, int* y) {
  int r1 = atomic_load_explicit(x, memory_order_acquire);
  if (r1) {
    *y = 1;
  }
}
